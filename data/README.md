# Synthetic financial data

The backend seeds deterministic synthetic transactions into `financial_copilot.db` on first startup. This directory is local-only prototype data and contains no Razorpay credentials or production records.