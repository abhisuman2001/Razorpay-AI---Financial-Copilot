import type { ReactNode } from "react";
import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { Area, CartesianGrid, ComposedChart, Line, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { Link } from "react-router-dom";
import { ArrowDownRight, ArrowRight, ArrowUpRight, Banknote, CheckCircle2, CircleAlert, Equal, HelpCircle, ReceiptIndianRupee, ShieldCheck, Sparkles, TrendingUp } from "lucide-react";

import { Sheet, SheetContent, SheetDescription, SheetHeader, SheetTitle } from "@/components/ui/sheet";
import { apiGet } from "@/lib/api";
import { formatDate, formatPaiseINR } from "@/lib/format";
import type { DailyInsight, ExecutiveDashboardResponse, ExecutiveMetric } from "@/lib/types";

interface ChartPoint {
  date: string;
  actual_balance: number | null;
  forecast_balance: number | null;
  lower: number | null;
  upper: number | null;
}

export default function Home() {
  const [selectedMetric, setSelectedMetric] = useState<ExecutiveMetric | null>(null);
  const dashboard = useQuery({
    queryKey: ["executive-dashboard"],
    queryFn: () => apiGet<ExecutiveDashboardResponse>("/executive/dashboard"),
    retry: false,
  });
  const data = dashboard.data;
  const chartData: ChartPoint[] = data ? [
    ...data.cashflow_actual.map((point) => ({
      date: point.date, actual_balance: point.cumulative_cash_balance,
      forecast_balance: null, lower: null, upper: null,
    })),
    ...data.cashflow_forecast.map((point) => ({
      date: point.date, actual_balance: null, forecast_balance: point.predicted_balance,
      lower: point.balance_lower, upper: point.balance_upper,
    })),
  ] : [];

  return (
    <div data-testid="overview-page">
      <section className="flex flex-col justify-between gap-5 border-b border-slate-200 pb-7 sm:flex-row sm:items-end" data-testid="executive-header">
        <div>
          <p className="mb-2 text-xs font-semibold uppercase tracking-[0.18em] text-rose-600" data-testid="overview-eyebrow">Executive overview</p>
          <h1 className="font-heading text-3xl font-bold tracking-[-0.04em] text-slate-950 sm:text-4xl" data-testid="overview-title">Good morning</h1>
          <p className="mt-2 font-heading text-lg font-medium text-slate-500 sm:text-xl" data-testid="overview-subtitle">Here's what needs your attention.</p>
        </div>
        <div className="text-left sm:text-right"><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-slate-400" data-testid="overview-data-label">Reconcile → Predict → Decide</p><p className="mt-1 font-mono text-xs font-medium text-slate-700" data-testid="overview-data-date">{data ? `Data as of ${formatDate(data.as_of_date)}` : "Loading financial context…"}</p></div>
      </section>

      {dashboard.isError ? <DataUnavailable /> : data ? <div className="mt-7 flex flex-col gap-7">
        <section className="order-2 grid gap-4 sm:grid-cols-2 xl:order-1 xl:grid-cols-4" data-testid="executive-metrics-grid">{data.metrics.map((metric) => <MetricCard key={metric.id} metric={metric} onExplain={() => setSelectedMetric(metric)} />)}</section>

        <section className="order-1 xl:order-3" data-testid="executive-insights-section">
          <div className="mb-4 flex items-end justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-rose-600" data-testid="executive-insights-eyebrow">Needs attention</p><h2 className="mt-2 font-heading text-xl font-bold tracking-tight text-slate-900" data-testid="executive-insights-title">AI CFO insights</h2></div><Link to="/cfo" className="hidden items-center gap-1.5 text-xs font-bold text-rose-700 transition-transform hover:translate-x-0.5 sm:inline-flex" data-testid="executive-insights-view-all">Ask AI CFO <ArrowRight size={13} /></Link></div>
          <div className="grid gap-4 lg:grid-cols-3" data-testid="executive-insights-grid">{data.insights.slice(0, 3).map((insight) => <ExecutiveInsightCard key={insight.id} insight={insight} />)}</div>
        </section>

        <section className="order-3 grid gap-5 xl:order-2 xl:grid-cols-[1.55fr_0.85fr]" data-testid="executive-analytics-grid">
          <div className="rounded-lg border border-slate-200 bg-white" data-testid="executive-cashflow-card">
            <div className="flex flex-col justify-between gap-3 border-b border-slate-100 p-5 sm:flex-row sm:items-center sm:p-6"><div><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400" data-testid="cashflow-section-eyebrow">Cash flow</p><h2 className="mt-2 font-heading text-lg font-bold text-slate-900" data-testid="cashflow-section-title">Actual & 30-day outlook</h2></div><div className="flex items-center gap-4 text-[10px] text-slate-500"><Legend color="bg-slate-500" label="60-day actual" testId="executive-legend-actual" /><Legend color="bg-rose-600" label="Forecast" testId="executive-legend-forecast" /><button type="button" onClick={() => setSelectedMetric(data.metrics.find((metric) => metric.id === "cash_forecast") ?? null)} className="inline-flex items-center gap-1 font-bold text-rose-700 transition-colors hover:text-rose-800" data-testid="cashflow-explain-button"><HelpCircle size={12} />Explain</button></div></div>
            <div className="h-[340px] p-3 sm:p-6" data-testid="executive-cashflow-chart"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={chartData} margin={{ top: 8, right: 4, left: 4, bottom: 0 }}><defs><linearGradient id="executiveBand" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#fda4af" stopOpacity={0.34} /><stop offset="100%" stopColor="#fda4af" stopOpacity={0.04} /></linearGradient></defs><CartesianGrid stroke="#eef2f6" vertical={false} /><XAxis dataKey="date" tickFormatter={(value: string) => value.slice(5)} minTickGap={28} tick={{ fontSize: 10, fill: "#94a3b8" }} axisLine={false} tickLine={false} /><YAxis tickFormatter={(value: number) => formatPaiseINR(value, true)} tick={{ fontSize: 10, fill: "#94a3b8" }} axisLine={false} tickLine={false} width={58} /><Tooltip content={<CashTooltip />} /><Area type="monotone" dataKey="upper" stroke="none" fill="url(#executiveBand)" connectNulls={false} /><Area type="monotone" dataKey="lower" stroke="#fecdd3" fill="#fff" fillOpacity={0.92} connectNulls={false} /><Line type="monotone" dataKey="actual_balance" stroke="#64748b" strokeWidth={2} dot={false} connectNulls={false} /><Line type="monotone" dataKey="forecast_balance" stroke="#e11d48" strokeWidth={2.5} dot={false} connectNulls={false} /></ComposedChart></ResponsiveContainer></div>
            <div className="flex items-center justify-between border-t border-slate-100 px-5 py-4 sm:px-6"><p className="flex items-center gap-2 text-[10px] text-slate-500" data-testid="cashflow-model-label"><ShieldCheck size={13} className="text-emerald-500" />Holt-Winters · 80% uncertainty range</p><Link to="/forecast" className="text-xs font-bold text-rose-700 transition-transform hover:translate-x-0.5" data-testid="cashflow-view-forecast-link">View forecast →</Link></div>
          </div>

          <div className="rounded-lg border border-slate-200 bg-white" data-testid="executive-reconciliation-card">
            <div className="border-b border-slate-100 p-5 sm:p-6"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400" data-testid="reconciliation-section-eyebrow">Reconciliation</p><h2 className="mt-2 font-heading text-lg font-bold text-slate-900" data-testid="reconciliation-section-title">Money movement health</h2></div><button type="button" onClick={() => setSelectedMetric(data.metrics.find((metric) => metric.id === "reconciliation_exceptions") ?? null)} className="inline-flex items-center gap-1 text-[10px] font-bold text-rose-700 transition-colors hover:text-rose-800" data-testid="reconciliation-explain-button"><HelpCircle size={12} />Explain</button></div><div className="mt-6 flex items-end justify-between"><div><p className="font-mono text-3xl font-medium text-slate-950" data-testid="executive-reconciliation-rate">{data.reconciliation.reconciliation_rate}%</p><p className="mt-1 text-[10px] text-slate-400" data-testid="executive-reconciliation-rate-label">Reconciliation rate</p></div><div className="flex h-10 w-10 items-center justify-center rounded-md bg-emerald-50 text-emerald-600"><CheckCircle2 size={19} /></div></div><div className="mt-4 h-1.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-emerald-500" style={{ width: `${data.reconciliation.reconciliation_rate}%` }} data-testid="executive-reconciliation-rate-fill" /></div></div>
            <div className="grid grid-cols-2 divide-x divide-y divide-slate-100" data-testid="executive-reconciliation-stats"><ReconStat label="Transactions analyzed" value={data.reconciliation.transactions_analyzed.toLocaleString("en-IN")} testId="recon-stat-analyzed" /><ReconStat label="Matched" value={data.reconciliation.matched.toLocaleString("en-IN")} testId="recon-stat-matched" tone="green" /><ReconStat label="Mismatched" value={data.reconciliation.mismatched.toLocaleString("en-IN")} testId="recon-stat-mismatched" tone="red" /><ReconStat label="Pending" value={data.reconciliation.pending.toLocaleString("en-IN")} testId="recon-stat-pending" tone="amber" /></div>
            <div className="border-t border-slate-100 p-5 sm:p-6"><p className="text-[10px] font-semibold uppercase tracking-[0.13em] text-slate-400" data-testid="executive-discrepancy-label">Total discrepancy</p><div className="mt-2 flex items-center justify-between"><p className="font-mono text-xl font-medium text-rose-700" data-testid="executive-discrepancy-value">{formatPaiseINR(data.reconciliation.total_discrepancy)}</p><Link to="/reconciliation" className="text-xs font-bold text-rose-700" data-testid="reconciliation-view-exceptions-link">View exceptions →</Link></div></div>
          </div>
        </section>
      </div> : <LoadingPanel />}

      <Sheet open={Boolean(selectedMetric)} onOpenChange={(open) => { if (!open) setSelectedMetric(null); }}>
        <SheetContent side="right" className="w-full overflow-y-auto p-0 sm:max-w-lg" data-testid="metric-explanation-panel">
          {selectedMetric && <><SheetHeader className="border-b border-slate-100 p-6 text-left"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-rose-600" data-testid="metric-explanation-eyebrow">Why this number?</p><SheetTitle className="font-heading text-xl" data-testid="metric-explanation-title">{selectedMetric.label}</SheetTitle><SheetDescription data-testid="metric-explanation-period">{selectedMetric.period_label}</SheetDescription></SheetHeader><MetricExplanation metric={selectedMetric} /></>}
        </SheetContent>
      </Sheet>
    </div>
  );
}

function MetricCard({ metric, onExplain }: { metric: ExecutiveMetric; onExplain: () => void }) {
  const visuals: Record<ExecutiveMetric["id"], { icon: ReactNode; color: string }> = { revenue: { icon: <ReceiptIndianRupee size={17} />, color: "bg-emerald-50 text-emerald-600" }, current_balance: { icon: <Banknote size={17} />, color: "bg-slate-100 text-slate-600" }, cash_forecast: { icon: <TrendingUp size={17} />, color: "bg-rose-50 text-rose-600" }, reconciliation_exceptions: { icon: <CircleAlert size={17} />, color: "bg-amber-50 text-amber-600" } };
  const visual = visuals[metric.id];
  const value = metric.value_kind === "currency" ? formatPaiseINR(metric.value, true) : metric.value.toLocaleString("en-IN");
  return <article className="group rounded-lg border border-slate-200 bg-white p-5 transition-transform hover:-translate-y-0.5 hover:shadow-md" data-testid={`executive-metric-${metric.id}`}><div className="flex items-center justify-between"><p className="text-xs font-medium text-slate-400" data-testid={`executive-metric-${metric.id}-label`}>{metric.label}</p><span className={`flex h-8 w-8 items-center justify-center rounded-md ${visual.color}`}>{visual.icon}</span></div><div className="mt-5 flex items-end justify-between gap-3"><p className="font-mono text-2xl font-medium tracking-tight text-slate-950" data-testid={`executive-metric-${metric.id}-value`}>{value}</p>{metric.change_percent !== null && <span className={`mb-1 inline-flex items-center gap-0.5 text-[10px] font-bold ${metric.trend === "up" ? "text-emerald-600" : "text-rose-600"}`} data-testid={`executive-metric-${metric.id}-change`}>{metric.trend === "up" ? <ArrowUpRight size={12} /> : <ArrowDownRight size={12} />}{Math.abs(metric.change_percent)}%</span>}</div><p className="mt-2 truncate text-[11px] text-slate-500" data-testid={`executive-metric-${metric.id}-detail`}>{metric.detail}</p><button type="button" onClick={onExplain} className="mt-5 inline-flex items-center gap-1.5 text-[10px] font-bold text-slate-500 transition-colors hover:text-rose-700" data-testid={`executive-metric-${metric.id}-why-button`}><HelpCircle size={12} />Why this number?</button></article>;
}

function MetricExplanation({ metric }: { metric: ExecutiveMetric }) { return <div className="space-y-6 p-6" data-testid="metric-explanation-content"><div className="rounded-lg bg-slate-950 p-5 text-white" data-testid="metric-explanation-value-card"><p className="text-[10px] uppercase tracking-[0.14em] text-slate-400" data-testid="metric-explanation-value-label">Current value</p><p className="mt-2 font-mono text-3xl font-medium" data-testid="metric-explanation-value">{metric.value_kind === "currency" ? formatPaiseINR(metric.value) : metric.value.toLocaleString("en-IN")}</p><p className="mt-2 text-xs text-slate-400" data-testid="metric-explanation-detail">{metric.detail}</p></div><div data-testid="metric-explanation-summary"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400">Explanation</p><p className="mt-2 text-sm leading-6 text-slate-600">{metric.explanation}</p></div><div className="rounded-lg border border-slate-200 bg-slate-50 p-4" data-testid="metric-calculation-card"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400">Calculation</p><p className="mt-2 font-mono text-[11px] leading-5 text-slate-700" data-testid="metric-calculation-text">{metric.calculation}</p></div><div data-testid="metric-source-card"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400">Source metric</p><div className="mt-2 flex items-center justify-between rounded-md border border-slate-200 p-3"><span className="text-xs font-medium text-slate-700" data-testid="metric-source-name">{metric.source_metric}</span><span className="font-mono text-[9px] text-slate-400" data-testid="metric-source-tool">{metric.source_tool}</span></div></div><div data-testid="metric-evidence-section"><div className="flex items-end justify-between"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400">Underlying records</p><span className="text-[9px] text-slate-400">Latest evidence</span></div><div className="mt-2 divide-y divide-slate-100 rounded-lg border border-slate-200">{metric.evidence.map((item) => <div key={item.id} className="grid grid-cols-[1fr_auto] gap-3 p-3" data-testid={`metric-evidence-${item.id}`}><div className="min-w-0"><p className="truncate font-mono text-[10px] text-slate-700" data-testid={`metric-evidence-${item.id}-id`}>{item.id}</p><p className="mt-1 truncate text-[10px] text-slate-400" data-testid={`metric-evidence-${item.id}-label`}>{item.type} · {item.label} · {formatDate(item.date)}</p></div><div className="text-right"><p className={`font-mono text-xs font-medium ${item.amount < 0 ? "text-rose-600" : "text-slate-800"}`} data-testid={`metric-evidence-${item.id}-amount`}>{formatPaiseINR(item.amount)}</p><p className="mt-1 text-[9px] uppercase tracking-wide text-slate-400" data-testid={`metric-evidence-${item.id}-status`}>{item.status.replaceAll("_", " ")}</p></div></div>)}</div></div><Link to={metric.drilldown_path} className="inline-flex w-full items-center justify-center gap-2 rounded-md bg-rose-600 px-4 py-3 text-xs font-bold text-white transition-colors hover:bg-rose-700" data-testid="metric-drilldown-link">Open detailed analysis <ArrowRight size={14} /></Link></div>; }

function ExecutiveInsightCard({ insight }: { insight: DailyInsight }) { const styles = { high: "bg-rose-50 text-rose-700", medium: "bg-amber-50 text-amber-700", low: "bg-emerald-50 text-emerald-700" }; const icons: Record<string, ReactNode> = { Reconciliation: <Equal size={17} />, "Cash flow": <TrendingUp size={17} />, Revenue: <ReceiptIndianRupee size={17} /> }; const path = insight.category === "Reconciliation" ? "/reconciliation" : insight.category === "Cash flow" ? "/forecast" : "/cfo"; return <article className="flex flex-col rounded-lg border border-slate-200 bg-white p-5 transition-transform hover:-translate-y-0.5 hover:shadow-md" data-testid={`executive-insight-${insight.id}`}><div className="flex items-center justify-between"><span className={`inline-flex items-center gap-1.5 rounded-full px-2 py-1 text-[9px] font-bold uppercase tracking-wide ${styles[insight.severity]}`} data-testid={`executive-insight-${insight.id}-severity`}>{insight.severity}</span><span className="text-rose-600">{icons[insight.category] ?? <Sparkles size={17} />}</span></div><h3 className="mt-4 font-heading text-base font-bold text-slate-900" data-testid={`executive-insight-${insight.id}-title`}>{insight.title}</h3><p className="mt-2 flex-1 text-xs leading-5 text-slate-500" data-testid={`executive-insight-${insight.id}-explanation`}>{insight.summary}</p><div className="mt-5 border-t border-slate-100 pt-4"><p className="text-[9px] font-semibold uppercase tracking-[0.13em] text-slate-400" data-testid={`executive-insight-${insight.id}-metric-label`}>{insight.metric_label}</p><p className="mt-1 font-mono text-lg font-medium text-slate-900" data-testid={`executive-insight-${insight.id}-metric-value`}>{insight.metric_value}</p><Link to={path} className="mt-4 inline-flex items-center gap-1.5 text-[10px] font-bold text-rose-700 transition-transform hover:translate-x-0.5" data-testid={`executive-insight-${insight.id}-action`}>{insight.action}<ArrowRight size={12} /></Link></div></article>; }

function ReconStat({ label, value, testId, tone = "slate" }: { label: string; value: string; testId: string; tone?: "slate" | "green" | "red" | "amber" }) { const colors = { slate: "text-slate-900", green: "text-emerald-700", red: "text-rose-700", amber: "text-amber-700" }; return <div className="p-4" data-testid={testId}><p className="text-[9px] uppercase tracking-[0.12em] text-slate-400" data-testid={`${testId}-label`}>{label}</p><p className={`mt-2 font-mono text-lg font-medium ${colors[tone]}`} data-testid={`${testId}-value`}>{value}</p></div>; }
function Legend({ color, label, testId }: { color: string; label: string; testId: string }) { return <span className="flex items-center gap-1.5" data-testid={testId}><span className={`h-2 w-2 rounded-full ${color}`} />{label}</span>; }
function CashTooltip({ active, payload, label }: { active?: boolean; payload?: Array<{ value?: number; dataKey?: string }>; label?: string }) { if (!active || !payload?.length) return null; const names: Record<string, string> = { actual_balance: "Actual", forecast_balance: "Forecast", lower: "Lower bound", upper: "Upper bound" }; return <div className="rounded-md border border-slate-200 bg-white p-3 text-xs shadow-lg" data-testid="executive-chart-tooltip"><p className="mb-2 text-slate-400">{label}</p>{payload.map((item) => <p key={item.dataKey} className="font-mono text-slate-700">{names[item.dataKey ?? ""]}: {formatPaiseINR(item.value ?? 0)}</p>)}</div>; }

function LoadingPanel() { return <div className="mt-7 rounded-lg border border-slate-200 bg-white p-8 text-sm text-slate-500" data-testid="overview-loading">Building your executive financial view…</div>; }
function DataUnavailable() { return <div className="mt-7 rounded-lg border border-amber-200 bg-amber-50 p-6" data-testid="overview-data-unavailable"><p className="font-heading font-bold text-amber-900" data-testid="overview-data-unavailable-title">Executive data is temporarily unavailable</p><p className="mt-1 text-sm text-amber-800" data-testid="overview-data-unavailable-copy">The workspace remains accessible. Refresh to reconnect to the deterministic finance services.</p></div>; }
